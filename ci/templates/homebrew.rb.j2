class Sad < Formula
  desc "{{ desc }}"
  homepage "{{ repo }}"
  version "{{ version }}"
  bottle :unneeded

  url "{{ x86_uri }}"
  sha256 "{{ x86_sha }}"

  depends_on "fzf"
  depends_on "git-delta"

  def install
    bin.install "sad"
  end
end
